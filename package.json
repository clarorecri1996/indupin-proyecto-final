{
  "name": "proyecto-final",
  "version": "1.0.0",
  "description": "proyecto de pintura",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss estilos/estilos.scss estilos/copilado.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
  },
  "author": "claudio",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.18"
  }
}
